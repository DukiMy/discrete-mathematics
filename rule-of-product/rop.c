#include <stdio.h>

// Create a table of products
int main() {
    // Take in parameters for symbols on the x axis and establish the length of that array of symbols.
    char**
    // Take in parameters for symbols on the y axis and establish the length of that array of symbols.

    // symbols must be one column wide, answers can be two columns wide.


    
    // Establish width and length

    return 0;
}